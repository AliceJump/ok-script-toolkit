name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  vscode:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify, compile, and test
        run: npm test

      - name: Package VSIX
        run: npm run package

      - name: Resolve package metadata
        id: version
        run: npm run --silent verify:version

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.version.outputs.vsix }}
          path: ${{ steps.version.outputs.vsix }}
          if-no-files-found: error
          retention-days: 14

  jetbrains:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Resolve package metadata
        id: version
        run: npm run --silent verify:version

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Test and package JetBrains plugin
        working-directory: jetbrains
        run: ./gradlew test buildPlugin verifyPluginStructure verifyPluginConfiguration

      - name: Upload JetBrains artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.version.outputs.jetbrains_zip }}
          path: jetbrains/build/distributions/${{ steps.version.outputs.jetbrains_zip }}
          if-no-files-found: error
          retention-days: 14
