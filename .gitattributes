# 行尾与二进制文件声明
#
# 背景：仓库在 Windows 上开发，core.autocrlf=true 会让检出时把文本文件转成 CRLF。
# shell 脚本一旦带上 CRLF，shebang 变成 `#!/usr/bin/env bash\r`，
# bash 会报 "bad interpreter: No such file or directory"。所以必须显式锁成 LF。

# shell 脚本必须 LF
*.sh text eol=lf

# Windows 批处理保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不做行尾转换，也避免 git diff 把它们当文本处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.vsix binary
*.zip binary

# 锁文件与编译产物
package-lock.json text eol=lf
*.jar binary
